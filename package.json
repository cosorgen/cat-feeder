{
  "name": "cat-feeder-game",
  "version": "1.0.0",
  "description": "A fun interactive cat feeding game with TypeScript",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3"
  }
}
